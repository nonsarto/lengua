# lengua backend — gepinnte Deps
anthropic>=0.40
python-dotenv>=1.0

# Slice 1 — Persistenz + API
fastapi>=0.115
uvicorn>=0.32
supabase>=2.9

# --- später (Sprache): ---
# openai>=1.50        # nur für Whisper-Transkription; Fotos brauchen KEIN extra OCR (Claude nimmt Bilder direkt)
